# 14 - 输出解析器

---

**本章课程目标：**

- 理解**输出解析器（Output Parser）**的作用与分类，以及 **parse**、**get_format_instructions()** 两大常用方法。
- 会使用 **StrOutputParser**、**JsonOutputParser** 将模型输出转为字符串或 JSON。
- 掌握**结构化输出**：理解官方三种方式 **Pydantic**、**TypedDict**、**JSON Schema**，会用 **TypedDict** / **Pydantic**（及 **Annotated** 写字段说明）与 `with_structured_output`、`PydanticOutputParser` 结合使用。

**前置知识建议：** 已学习 [第 11 章 Model I/O 与模型接入](11-Model-I-O与模型接入.md)、[第 13 章 提示词与消息模板](13-提示词与消息模板.md)，了解 Model I/O 三件套中的「输入」与「模型调用」，便于与本章「输出解析」形成完整链路。

---

## 1、输出解析器概述

本章对应 [Model I/O](11-Model-I-O与模型接入.md) 中的**输出**部分：把模型的**文本输出**转成程序易用的**结构化数据**（如字符串、JSON、强类型对象）。与 [第 13 章 提示词](13-提示词与消息模板.md)（输入）、第 11 章模型（调用）组合，即形成「输入 → 模型 → 输出解析」完整链路，[第 15 章 LCEL](15-LCEL与链式调用.md) 会用管道符将三者串成一条链。

- **Output Parsers API**：https://reference.langchain.com/python/langchain_core/output_parsers/
- **结构化输出（Structured Output）**：https://docs.langchain.com/oss/python/langchain/models#structured-output

### 1.1 为什么需要输出解析器

大模型返回的通常是**纯文本**。而在实际应用中，我们往往需要**固定格式**的数据（如 JSON、列表、对象）以便做后续逻辑、入库或展示。

**输出解析器（Output Parser）** 负责把模型的原始输出转换成所需格式，并可在解析失败时进行重试或修复，是 Model I/O 中不可或缺的一环。

![输出解析器在 Model I/O 中的位置与作用](images/14/14-1-1-1.jpeg)

---

### 1.2 什么是输出解析器

**输出解析器**是 LangChain 中位于「模型输出」和「最终数据」之间的**中间层**，主要作用包括：

- **指定格式输出**：将模型文本转换为 JSON、XML、YAML 等目标格式。
- **数据校验**：确保输出符合预期的结构和类型。
- **错误处理**：解析失败时进行重试或修正。
- **格式说明注入**：通过 **get_format_instructions()** 等方法生成「希望模型输出何种格式」的说明文本，拼入提示词，引导模型直接生成可解析的内容。

### 1.3 输出解析器分类与常用方法

**分类概览**：LangChain 提供多种输出解析器，常用分类如下（第 2 节将按本表逐一介绍各解析器的**具体用法与示例**）。

| 解析器                   | 输出形式 / 适用场景 | 说明                                            |
| ------------------------ | ------------------- | ----------------------------------------------- |
| **StrOutputParser**      | 纯字符串            | 从模型返回中取 content 转成 str，不做结构解析。 |
| **JsonOutputParser**     | JSON                | 将模型文本解析为 JSON（dict/list）。            |
| **PydanticOutputParser** | Pydantic 模型       | 按 Pydantic 模型定义解析，得到强类型对象。      |

**两大常用方法**：

- <strong>parse(result)</strong>：将模型返回的<strong>原始结果</strong>（如 AIMessage）解析成<strong>指定格式</strong>并返回（如 dict、str、Pydantic 对象）。
- <strong>get_format_instructions()</strong>：返回一段<strong>格式说明字符串</strong>，描述「希望模型输出成什么样子」（如 JSON 的键与类型）。把这段说明拼进 Prompt，可提高模型输出可解析的成功率。

---

## 2、常用输出解析器用法

下面按 **1.3** 中的分类，逐一介绍 StrOutputParser、JsonOutputParser 等的**具体用法与示例**（Pydantic 相关用法见第 3 节结构化输出）。

### 2.1 StrOutputParser（字符串解析器）

LangChain 中最简单的输出解析器，从模型返回中取出 **content** 字段并转为字符串，不做额外结构解析。

【案例源码】`案例与源码-2-LangChain框架/05_parser/StrOutputParserDemo.py`

[StrOutputParserDemo.py](案例与源码-2-LangChain框架/05_parser/StrOutputParserDemo.py ":include :type=code")

### 2.2 JsonOutputParser（JSON 解析器）

将模型的**自由文本输出**解析为**结构化 JSON**。

**两种常见用法**：

1. **在提示词中直接写明**「请以 JSON 格式返回，包含字段 xxx、yyy」。
2. <strong>借助 JsonOutputParser.get_format_instructions()</strong>：用该方法得到一段格式说明，拼进 Prompt，让模型按该格式输出，再用 `JsonOutputParser().parse(...)` 解析。

【案例源码】`案例与源码-2-LangChain框架/05_parser/JsonOutputParserDemo.py`（手写提示词要求返回 JSON）、`案例与源码-2-LangChain框架/05_parser/JsonOutputParser_GetFormatInstructions.py`（使用 get_format_instructions 拼进提示词）。

[JsonOutputParserDemo.py](案例与源码-2-LangChain框架/05_parser/JsonOutputParserDemo.py ":include :type=code")

[JsonOutputParser_GetFormatInstructions.py](案例与源码-2-LangChain框架/05_parser/JsonOutputParser_GetFormatInstructions.py ":include :type=code")

---

## 3、结构化输出（Pydantic / TypedDict / JSON Schema）

当希望模型返回**固定格式**的数据（例如「一定包含 name、age 两个字段的字典」），而不仅是随便一段话时，可以用「结构化输出」：先定义好「长什么样」，再让 LangChain 按这个形状去引导模型输出并解析。

[Structured output](https://docs.langchain.com/oss/python/langchain/models#structured-output) 将结构化输出分为三种方式：**Pydantic**、**TypedDict**、**JSON Schema**。在 Python 里定义「长什么样」时，还会用到 **Annotated** 来给字段加描述（常与 TypedDict / Pydantic 配合），它不是第三种 schema 类型，而是「写字段说明」的写法。下面先辨析 TypedDict、Pydantic、Annotated 这些概念，再按 Pydantic / TypedDict 讲用法；JSON Schema 方式见官方文档。

- **官方文档**：https://docs.langchain.com/oss/python/langchain/models#structured-output

### 3.1 概念辨析

| 名字          | 从哪来                                  | 通俗理解                                                                                                                                                                                                                                          | 会不会在「运行时」做校验？                 |
| ------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| **TypedDict** | Python **标准库** `typing`（3.8+）      | 用来**描述**一个字典「有哪些键、每个键是什么类型」。相当于给 dict 写一张「结构说明书」，给人或工具（如 LangChain）看，**不会**在运行时报错说「你多写了一个键」或「值类型不对」。                                                                  | 否，仅类型提示/说明。                      |
| **Pydantic**  | **第三方库**，需 `pip install pydantic` | 用来定义「带校验的数据模型」：你写一个类（继承 BaseModel），声明字段和类型；**创建对象时**会真的检查类型、长度、范围等，不合格就抛 `ValidationError`。                                                                                            | 是，会按 Field、validator 等校验。         |
| **Annotated** | Python **标准库** `typing`（3.9+）      | 用来在类型上**挂一段额外信息**（元数据），例如「这个 int 表示年龄，范围 0–150」。这段信息可以给 LangChain 用来生成提示词里的字段说明；在**纯 Python** 里不会自动变成「校验规则」。只有和 **Pydantic 的 Field** 一起用时，才会变成真正的范围校验。 | 单独用：否。和 Pydantic Field 一起用：是。 |

**一句话记**：**TypedDict** 和 **Annotated** 是标准库里的「说明/注释」；**Pydantic** 是第三方库，负责「真刀真枪」的校验和强类型。做结构化输出时，可以用 TypedDict 或 Pydantic 定义「长什么样」，LangChain 再根据这个定义去让模型按格式输出并解析。

---

### 3.2 TypedDict：用标准库描述「字典长什么样」

**TypedDict** 来自 Python 自带的 `typing`，用来声明「一个字典里有哪些键、分别是什么类型」。它**不负责**在运行时检查你传进去的 dict 是否真的符合，主要作用是：

- 给人、IDE、LangChain 看：模型输出的 JSON 应该长什么样；
- LangChain 可以根据这个「形状」生成提示词里的格式说明，并把模型返回的 JSON 解析成 Python 的 dict。

**结合案例看**（`StructuredOutput_TypedDict.py`）：先定义「一个动物」和「动物列表」的结构，再用 `llm.with_structured_output(AnimalList)` 让模型按这个结构返回并自动解析。

【案例源码】`案例与源码-2-LangChain框架/05_parser/StructuredOutput_TypedDict.py`

[StructuredOutput_TypedDict.py](案例与源码-2-LangChain框架/05_parser/StructuredOutput_TypedDict.py ":include :type=code")

**典型步骤小结**：① 用 `class XXX(TypedDict):` 声明字段；② 可选地用 `Annotated[类型, "描述"]` 给字段加说明；③ `llm.with_structured_output(XXX)` 得到「按 XXX 输出并解析」的可调用对象；④ `.invoke(messages)` 得到解析好的 dict。

---

### 3.3 Annotated 与 Pydantic：描述 vs 校验

**Annotated** 是在类型上「加一段说明」，例如 `Annotated[int, "年龄，范围0-150"]`。在**纯 Python + TypedDict** 里，这段说明**不会**在运行时帮你检查「年龄是不是真的在 0–150」——Python 只会关心「是不是 int」。所以你可以写 `age2=188` 而不会报错。案例见 `AnnotatedTypedDict.py`：

【案例源码】`案例与源码-2-LangChain框架/05_parser/AnnotatedTypedDict.py`

[AnnotatedTypedDict.py](案例与源码-2-LangChain框架/05_parser/AnnotatedTypedDict.py ":include :type=code")

**Pydantic** 则会在**创建对象时**做校验。若用 Pydantic 的 `Field(ge=0, le=150)` 和 Annotated 结合，例如 `Annotated[int, Field(ge=0, le=150, description="年龄")]`，那么赋值 188 就会抛 `ValidationError`。案例见 `AnnotatedPydantic.py`：

【案例源码】`案例与源码-2-LangChain框架/05_parser/AnnotatedPydantic.py`

[AnnotatedPydantic.py](案例与源码-2-LangChain框架/05_parser/AnnotatedPydantic.py ":include :type=code")

**Pydantic 做结构化输出**：用 Pydantic 定义模型（如 Product：name、category、description），再用 `PydanticOutputParser(pydantic_object=Product)` 和 `get_format_instructions()` 把格式说明拼进 Prompt，模型返回后解析成 Product 实例，并可做长度、范围等校验。完整流程见 `StructuredOutput_Pydantic.py`。

**使用 Annotated 增强类型说明**：在 TypedDict 或 Pydantic 的字段里用 `Annotated[类型, "描述"]` 或 `Annotated[类型, Field(...)]`，可以让 LangChain 生成更清晰的格式说明，模型更容易按字段含义输出。

【案例源码】Pydantic 结构化输出完整流程：`案例与源码-2-LangChain框架/05_parser/StructuredOutput_Pydantic.py`

[StructuredOutput_Pydantic.py](案例与源码-2-LangChain框架/05_parser/StructuredOutput_Pydantic.py ":include :type=code")

---

### 3.4 JSON Schema（官方第三种方式）

官方文档中的第三种结构化输出方式是 **JSON Schema**：直接传入一个符合 [JSON Schema](https://json-schema.org/understanding-json-schema/about) 的 `dict`，通过 `model.with_structured_output(json_schema, method="json_schema")` 让模型按该 schema 输出并解析。适合需要与外部约定格式、或不想依赖 Pydantic/TypedDict 的场景。用法与示例见 [Structured output - JSON Schema](https://docs.langchain.com/oss/python/langchain/models#structured-output) 对应 Tab。

---

**本章小结：**

- <strong>输出解析器</strong>：把模型返回的纯文本转为程序可用的结构化数据；常用 <strong>parse(result)</strong> 与 <strong>get_format_instructions()</strong>（将格式说明拼入 Prompt）。
- **常用解析器**：**StrOutputParser**（取 content 为字符串）、**JsonOutputParser**（解析 JSON）、**PydanticOutputParser**（按 Pydantic 模型解析）。**结构化输出**官方分三种：**Pydantic**、**TypedDict**、**JSON Schema**；用 **Annotated** 可为 TypedDict/Pydantic 字段加描述，便于模型按格式输出。
- 与 [第 11 章](11-Model-I-O与模型接入.md) Model I/O、[第 13 章](13-提示词与消息模板.md) 提示词与消息模板结合：**输入**用第 13 章的 Prompt 与模板，**模型**用第 11 章的接入方式，**输出**用本章的 Parser，即可形成完整的「提问 → 推理 → 结构化结果」链路。三者用 [第 15 章 LCEL 与链式调用](15-LCEL与链式调用.md) 串联成 `prompt | model | parser` 链即可一步到位。

**建议下一步：** 学习 [第 15 章 LCEL 与链式调用](15-LCEL与链式调用.md)，将 [第 11 章](11-Model-I-O与模型接入.md) 的模型、[第 13 章](13-提示词与消息模板.md) 的提示词与本章的解析器用管道符 `|` 组合成链，实现「输入 → 模型 → 输出解析」一键调用。

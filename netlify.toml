# Docsify：无构建，直接发布仓库根目录静态文件
# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  publish = "."
  command = "echo 'Static Docsify; no build'"

[build.environment]
  PYTHON_VERSION = "3.10"
